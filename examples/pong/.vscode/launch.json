
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "brightscript",
      "request": "launch",
      "name": "run",
      "stopOnEntry": false,
      "host": "${promptForHost}",
      "password": "${promptForPassword}"
    }
  ]
}