{
  "private": true,
  "scripts": {
    "build": "bsc",
    "clean": "rimraf dist && rimraf out",
    "package": "bsc --create-package"
  },
  "ropm": {
    "rootDir": "src"
  },
  "devDependencies": {
    "brighterscript": "^0.40.0",
    "rimraf": "^3.0.2",
    "ropm": "^0.9.1"
  }
}
