{
  "private": true,
  "scripts": {
    "build": "bsc",
    "clean": "rimraf dist && rimraf out",
    "package": "bsc --create-package"
  },
  "ropm": {
    "rootDir": "src"
  },
  "dependencies": {
    "bge": "file:../../brighterscript-game-engine-0.1.0.tgz"
  },
  "devDependencies": {
    "brighterscript": "^0.34.3",
    "rimraf": "^3.0.2",
    "ropm": "^0.9.1"
  }
}
